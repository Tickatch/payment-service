spring:
  config:
    import: "optional:"
  cloud:
    config:
      enabled: false

app:
  base-url: http://localhost:8080
  frontend-url: http://localhost:8080

toss:
  secret-key: dummy_secret_for_test